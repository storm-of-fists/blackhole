[package]
name = "nucleus_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
nucleus = { path = "../nucleus" }

[lib]
crate-type = ["lib"]

[[bench]]
name = "main"
harness = false

[dev-dependencies]
criterion = "0.5.1"