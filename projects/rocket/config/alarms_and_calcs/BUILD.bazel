load("//common:defs.bzl", "config")

package(default_visibility = ["//visibility:public"])

config(
    name = "alarms_and_calcs",
    srcs = glob(["*.yaml"]),
    yaml_config = ["//projects/rocket/config:imu_config"],
)
